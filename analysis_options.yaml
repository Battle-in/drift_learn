include: package:flutter_lints/flutter.yaml

linter:

  rules:
    # avoid_print: false  # Uncomment to disable the `avoid_print`
    # prefer_single_quotes: true  # Uncomment to enable the
    # `prefer_single_quotes` rule
    lines_longer_than_80_chars: true

# Additional info: https://dart.dev/guides/language/analysis-options
